public class ProfitLeet121 {
    public static void main(String[] args) {

    }
/*
to improve user experience by remembering them, like we log in to a website once, and we can visit again and again, without login each time.
 */
    public int maxProfit(int[] arr) {
        int profit = 0, lp = 0, hp = 1;
        // one variable initialization required;
        int var = 0;
        for (int hp = 0; hp < ; hp++) {

        }

        if (arr[lp] < arr[hp]) {
            // var = Math.max(,);
            profit = arr[lp] - arr[hp];
        } else {
            // assignment;
        }
        return profit;
    }
}
